# Python-hashing-program-
A tiny simple python program that hashes the text you want in sha256 algorithm plus a randomly 16 digit salt making it secure.
A user inputs the text then the program using the hashlib function performs the hashing.

